# تقرير تنفيذ تعدد اللغات — Smart Agency

**تاريخ التنفيذ:** 24 أغسطس 2026  
**النطاق المنفذ:** جميع الأعمال الناقصة خارج المرحلة الثالثة  
**الاستثناء الصريح:** لم تُنفذ أي عملية نسخ احتياطي أو ترحيل أو تعديل يدوي لبيانات MongoDB.

## الملخص التنفيذي

تم تنفيذ البنية ثنائية اللغة للواجهة العامة والـ API، وربط اللغة بالمسار والاتجاه وطلبات الشبكة والكاش والـ SEO. كما تم تنظيف طبقة المحتوى القديمة وإضافة قواعد تمنع نشر محتوى جديد غير مكتمل بالإنجليزية.

لم يُعتمد رقم التدقيق الأصلي فقط؛ فقد احتوى التقرير على 444 نصاً عاماً، بينما كشف فحص المصدر عن 215 نصاً إضافياً لم يشملها الاستخراج السابق. أضيفت المجموعتان إلى موارد الترجمة وربطت النصوص العامة بها.

## المرحلة الرابعة — API عامة واعية باللغة

- إضافة محلل مركزي للغتين `ar` و`en` بأولوية `?lang=` ثم `Accept-Language` ثم العربية.
- إضافة محول مركزي للاستجابات العامة يعيد الحقول الإنجليزية تحت الاسم العام نفسه، ويحذف حقول `*En` من الاستجابة العامة.
- تطبيق سياسة fallback في الاتجاهين، مع أولوية العربية للطلبات العربية والإنجليزية للطلبات الإنجليزية.
- إبقاء استجابات الإدارة محتوية على الحقول العربية والإنجليزية معاً عبر ترويسة داخلية مخصصة لطلبات لوحة التحكم.
- تحديث إسقاطات MongoDB العامة لتجلب الحقول الإنجليزية قبل التوطين، بما يشمل الصفحة الرئيسية والمشاريع والمدونة والخدمات والأسئلة والاستضافة والفريق والتقنيات وآراء العملاء وبيانات الشركة.
- توسيع البحث في المدونة والمشاريع والخدمات والأسئلة ليشمل الحقول العربية والإنجليزية.
- جعل تصنيفات وتاغات المدونة واعية باللغة مع fallback.
- توسيع فهارس البحث النصي للمدونة والمشاريع لتشمل الحقول الإنجليزية.
- إضافة رموز أخطاء ثابتة إلى عقد الأخطاء العامة.
- إضافة اختبارات وحدة لمحلل اللغة والتوطين المتداخل وسياسة fallback.

## المرحلة الخامسة — المسارات والواجهة وi18n

- تثبيت وتهيئة `i18next` و`react-i18next`.
- إنشاء موارد عربية وإنجليزية تضم:
  - 444 نصاً من حزمة التدقيق الأصلية.
  - 218 مفتاحاً إضافياً اكتشفها فحص المصدر، منها ثلاثة مفاتيح أضيفت لتصحيح رموز خيارات نموذج العرض.
- ربط النصوص العامة بموارد الترجمة؛ فحص AST النهائي لم يجد نصوصاً عربية عامة غير مغلفة خارج موارد اللغة.
- إضافة المسارات العامة تحت `/ar` و`/en` لكل الصفحات العامة الحالية.
- إضافة تحويلات توافقية للروابط القديمة إلى النسخة العربية مع الحفاظ على query string وhash.
- إضافة مبدل لغة للحاسوب والهاتف يحافظ على الصفحة الحالية.
- ربط `lang` و`dir` في عنصر HTML والـ body بلغة المسار.
- إزالة قيم RTL الثابتة من مكونات الواجهة العامة؛ الفحص النهائي لم يجد `dir="rtl"` ثابتاً فيها.
- إضافة اللغة تلقائياً إلى طلبات GET العامة وإلى بيانات leads/newsletter/package selection.
- عزل كاش React Query حسب اللغة، وتضمين اللغة صراحة في مفاتيح الصفحة الرئيسية وبيانات الشركة.
- تصحيح نموذج عرض السعر ليخزن booleans والرموز الثابتة (`android`, `both`, `mvp`, `full_product`) بدلاً من القيم المترجمة.

## المرحلة السادسة — SEO والتنظيف وقواعد النشر

- إضافة إدارة ديناميكية لكل من title وdescription وcanonical وhreflang (`ar`, `en`, `x-default`).
- إضافة Open Graph وTwitter metadata وبيانات Organization المنظمة حسب اللغة.
- إضافة sitemap ثنائي اللغة للمسارات العامة الأساسية وتحديث robots.txt للإشارة إليه.
- إضافة manifest إنجليزي مستقل وتحديث manifest العربي ليبدأ من `/ar`.
- حذف ملفات المحتوى القديمة غير المستخدمة من `frontend/src/data`.
- إضافة قاعدة نشر للمشاريع والمدونات الجديدة، وكذلك الانتقال من المسودة إلى المنشور، لمنع النشر إذا كانت الحقول الإنجليزية وSEO الإنجليزي الأساسي غير مكتملين.
- تحديث اختبار الصحة القديم الذي كان ما يزال يتوقع `Hello World` ليفحص endpoint الصحة الحالي.

## ما لم يُنفذ

### المرحلة الثالثة — ترحيل البيانات

لم يتم تنفيذ أي بند منها، طبقاً لطلب المالك:

- لا نسخ احتياطي لقاعدة MongoDB.
- لا استيراد لترجمات CMS.
- لا تعديل لوثائق المشاريع أو التصنيفات أو التقنيات في قاعدة البيانات.
- لا حساب لأوقات القراءة الإنجليزية على السجلات الموجودة.
- لا تصحيح يدوي لأسماء الأشخاص أو نص Tajaddod أو `ٍSaaS` أو `Garfana` داخل البيانات.

### الفحص البصري

لم يُنفذ الفحص البصري النهائي بناءً على توجيه المالك بأن هذه المهمة ستُنفذ يدوياً من جانبه.

## نتائج التحقق الآلي

| الفحص | النتيجة |
|---|---|
| `frontend: npm run build` | ناجح — TypeScript وVite |
| `frontend: npm run lint` | ناجح — 0 أخطاء |
| `backend: npm run build` | ناجح — Nest build |
| `backend: eslint` | ناجح — 0 أخطاء، 24 تحذيراً |
| `backend: npm test -- --runInBand` | ناجح — مجموعتان، 4 اختبارات |
| فحص النصوص العامة الخام | ناجح — لا نصوص عربية عامة غير مربوطة بـ i18n |
| فحص RTL الثابت في الواجهة العامة | ناجح — لا نتائج |

تحذيرات ESLint الخلفية المتبقية غير حاجبة للبناء، وهي تحذيرات `no-unsafe-argument` موروثة من استعمال `any` في كائنات استعلام Mongoose داخل خدمات متعددة. لا توجد أخطاء Lint متبقية.

## خطوات المالك قبل الإطلاق

1. تنفيذ المرحلة الثالثة يدوياً بعد أخذ نسخة احتياطية.
2. مراجعة الأسماء اللاتينية المعتمدة وأعلام جودة المحتوى الواردة في حزمة التدقيق.
3. تنفيذ الفحص البصري اليدوي بالعربية والإنجليزية على الهاتف والحاسوب.
4. التأكد من ضبط `VITE_SITE_URL` على نطاق الإنتاج إن كان مختلفاً عن `https://smartagency-ye.com`.
5. بعد اكتمال الترحيل، تجربة نشر سجل مشروع ومقال جديدين للتأكد من أن قاعدة الاكتمال تناسب سياسة فريق المحتوى.
